import styled from 'styled-components';

export const Header = styled.header`
  background: var(--mid-purple);
  padding: 1em 5em;
  span {
    font-weight: bold;
    color: #ebebeb;
  }
`;
