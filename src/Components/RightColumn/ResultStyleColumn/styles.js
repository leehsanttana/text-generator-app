import styled from 'styled-components';

export const ResultStyleColumn = styled.section`
  border: 2px solid var(--light-blue);
  border-radius: 4px;
  padding: 2rem;
  font-family: Arial, Helvetica, sans-serif;
  overflow-y: scroll;
  height: 300px;
`;
