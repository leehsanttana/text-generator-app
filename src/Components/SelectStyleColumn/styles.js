import styled from 'styled-components';

export const SelectStyleColumn = styled.section`
  background: var(--dark-purple);
  border: 2px solid var(--light-blue);
  border-radius: 4px;
  padding: 2em;
  overflow-y: scroll;
  max-height: 645px;
`;
